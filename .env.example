DATABASE_URL="postgresql://acm:acm_dev_pass@localhost:5432/acm_dev"
REDIS_URL="redis://localhost:6379"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="dev-secret-change-in-production"

SEED_DEFAULT_PASSWORD="pass@1234"

# Google OAuth (optional for local dev)
# GOOGLE_CLIENT_ID=""
# GOOGLE_CLIENT_SECRET=""

# SMTP — required. For local dev, use Mailpit via docker-compose.dev.yml
# Web UI at http://localhost:8025
SMTP_HOST="localhost"
SMTP_PORT="1025"
SMTP_FROM="acm-dev@localhost"
# SMTP_USERNAME=""
# SMTP_PASSWORD=""

# CallPro Teams integration — leave blank to disable provisioning locally
CALLPRO_TEAMS_API_URL=""
CALLPRO_TEAMS_API_KEY=""
